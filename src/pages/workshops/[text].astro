---
import Layout from "../../layout/Layout.astro";
const { text } = Astro.params;
if (!/^[a-zA-Z]+$/.test(text)) {
  return Astro.redirect('/error');
}
---

<Layout route="workshops" title="Cexpe | Talleres">
	<div class="h-20"/>
	<h1>Talleres</h1>
  	<p>Este es el contenido de la página de Talleres</p>
	<p>Texto: {text}</p>
</Layout>